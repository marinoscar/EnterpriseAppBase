import { Box, Typography } from '@mui/material';

export default function LoginPage() {
  return (
    <Box>
      <Typography variant="h4">Login Page</Typography>
      <Typography>This page will be implemented in a later spec.</Typography>
    </Box>
  );
}
